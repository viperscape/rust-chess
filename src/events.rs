use std::sync::mpsc::{channel,Sender,Receiver};

pub struct Events;
impl Events {
   /* pub fn new<F,F2> (fncomm: F, fninput: F2)
        where F: FnMut{
        
    }
    
    pub fn with (&self) { ; }*/
}
